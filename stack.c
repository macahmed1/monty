#include "monty.h"
/**
 * _stack - Set the mode to stacking
 * @stack: the stack
 * @line: line number
 */
void _stack(stack_t **stack, unsigned int line)
{
	 (void) stack;
	 (void) line;
	global.mode = 0;
}
